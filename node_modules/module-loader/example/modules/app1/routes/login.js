module.exports = function (app, r) {

  r.addRoute('', 'get', [
    function (req, res) {
      res.send('login');
    }
  ]);

  r.addRoute('login/:id', 'post', [
    function (req, res) {
      res.send('login with id');
    }
  ]);

};
